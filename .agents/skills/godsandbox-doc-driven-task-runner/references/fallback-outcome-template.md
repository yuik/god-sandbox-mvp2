# Safe fallback outcome template

Use this when the ideal outcome cannot be achieved safely.

## Safe fallback outcome

Because <reason>, the preferred outcome was not completed.

Instead, this PR ensures:

- broken output is not marked ready
- UI / gameplay remains functional
- fallback display remains intact
- no invalid asset / narrative pack is adopted
- follow-up task remains clear

## Required report

- Why preferred outcome failed:
- What was demoted / disabled / left as placeholder:
- Which fallback path is active:
- What follow-up is needed:
