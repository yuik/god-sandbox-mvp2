# ryo extended sprite sheet prompt (Sheet 2)

You are creating Sheet 2 (extended sprite sheet) for a GodSandbox resident character.
Sheet 2 provides 2.5D directional walks and emotional emotes not covered by Sheet 1.

Workflow note:
- This prompt is for Codex hatch-pet generation controlled by the Codex Sidekick.
- GodSandbox must not call an image generation API from inside the app.
- Do not include API keys, account details, personal paths, or billing details in the output.

Generation source requirement:
- Generate with Codex hatch-pet. Incoming placement is allowed only after `npm run sidekick:resident:hatch-pet` validates hatch-pet final output.
- Raw Image Gen output is evidence only. Do not copy raw output to incoming.
- Use the same visual style as Sheet 1 for ryo for consistency.
- If generation is unavailable, stop and report `generation step unavailable`.
- Do not create a local handmade, synthetic, placeholder, or validation-only sprite sheet as a substitute.

Reference:
- Use `public/art/characters/defaults/ryo/portrait.png` only as the identity reference.
- Match the Sheet 1 sprite appearance for style and proportions.
- Do not crop, resize, or paste the portrait into the sprite sheet.

Character handling:
- Match the hair, outfit colors, silhouette, and overall mood from Sheet 1 exactly.
- Do not add unrelated accessories or change the character design.

Style:
- Same style as Sheet 1: cute small pixel art, chibi / SD game character
- transparent background (or flat `#ff00ff` chroma-key if true alpha unavailable)
- clean outline, consistent scale and baseline
- readable at 192x208 px

Visual correctness:
- Each 192x208 frame must contain one complete character.
- Keep head, torso, and feet inside the same frame.
- Do not let the body cross into a neighboring row or column.
- Keep at least about 10px left/right margin and 8px top/bottom margin when possible.
- Emote particles (sparkles, sweat drops, etc.) are allowed but must not look like detached body parts.
- Do not include labels, frame numbers, guide lines, or any square background.

Sprite sheet spec (Sheet 2 — extended-sheet):

**REQUIRED canvas size: 1536 × 1872 px (width × height). This is mandatory.**
**REQUIRED frame size: 192 × 208 px per cell. 8 columns, 9 rows.**
If your tool generates a different size, the output will fail validation. Generate at exactly 1536 × 1872 px.
- Downloaded PNG must go through: `assets/generated/residents/ryo/incoming/`
- Adopted output: `public/art/characters/defaults/ryo/sprites/resident-sprite-sheet-extended.png`
- PNG with real alpha channel (or `#ff00ff` chroma-key background)
- Canvas: **1536 × 1872 px** (= 8 columns × 192 px, 9 rows × 208 px) — generate at EXACTLY this size
- frame size: 192 x 208 px (non-square, same as Sheet 1)
- columns: 8
- rows: 9 (rows 0–7 are used; row 8 may be left empty)
- each row is one motion, each column is one animation frame
- no labels, numbers, UI frames, or background inside the image

Motion rows (Sheet 2):
row 0: `walk-up` — walking away from viewer, back view, 2.5D up direction
row 1: `walk-down` — walking toward viewer, front view, 2.5D down direction
row 2: `walk-forward` — walking straight toward viewer, front-center with slight depth
row 3: `walk-back` — walking straight away from viewer, back-center with slight depth
row 4: `emote-happy` — joyful bounce or cheer, expressive happiness
row 5: `emote-angry` — crossed arms or stomp, expressive frustration
row 6: `emote-sad` — drooping shoulders or wiping tears, expressive sadness
row 7: `emote-surprised` — jump or wide eyes, expressive surprise
row 8: `spare` — leave transparent or duplicate row 7

Use only this row manifest for Sheet 2. If your tool cannot follow it exactly, stop and report `generation step unavailable`.

Important:
- This must be a real sprite sheet generated by your image generation tool.
- Do not use a resized portrait as the sprite.
- Keep every frame recognizable as ryo.
- Maintain strict visual consistency with Sheet 1 (same art style, same character proportions).
- Every frame must stand on its own without borrowing pixels from neighboring cells.
- This prompt is for ryo. For other characters use their own prompt file.
- If generation is unavailable, do not create a substitute. Stop and report `generation step unavailable`.
